# 周记（第 4 周）

**时间**：2025-12-16 ～ 2025-12-22
**课题**：基于高斯泼溅的三维场景表示与生成研究
**学生**：翟国富（922130830318）

---

## 本周目标

1. 明确毕设主线：以三维场景重建为核心，优先跑通公开数据集 A1（NeRF Synthetic）的 3DGS 基线闭环。
2. 完成本地开发环境搭建，验证"可微渲染 + 反向传播"在高斯表示上的基本可行性。
3. 准备云端（Kaggle/Colab）训练环境方案，为下周正式跑 3DGS 做铺垫。

## 本周完成情况

### 1）课题理解与路线决策
- 结合导师任务书，确认"重建/表示"为硬主线，"生成（diffusion）"作为后续扩展或相关工作切入点。
- 确定数据优先级：先用公开 A1（NeRF Synthetic）跑通 train→render→metrics，再扩展到真实数据集（T&T/Mip-NeRF 360）。

### 2）本地环境与概念验证
- 在 Mac 上采用 venv 方案完成 Python 环境创建与依赖安装，绕开 conda ToS 问题，保证项目级环境隔离。
- 运行 toy 版 3DGS MVP（简化可微 splat 渲染），确认：高斯参数（位置/尺度/颜色/透明度）可以通过渲染误差反向传播被优化，loss 随训练下降，输出结果逐步逼近目标视角渲染。

### 3）云端执行方案准备（未完全落地）
- 拟定在 Kaggle Notebook 上运行 3DGS 的流程：启用 GPU、拉取仓库、安装/编译依赖、挂载/获取 NeRF Synthetic、设置场景路径 SCENE、进行 smoke run（小迭代数）验证闭环。
- 发现 Kaggle 中 shell 命令需用 `!` 或 `%%bash` 运行，已修正基本用法错误（将 find 命令误当作 Python 语句导致 SyntaxError）。

## 关键结果/产出

- **路线产出**：确定"先重建后生成"的实施顺序，锁定 A1 作为第一条可验收实验闭环。
- **工程产出**：本地可复现的最小 toy 实验（能够体现 GS 训练范式），并完成环境搭建记录（venv）。
- **风险提示**：官方 3DGS 训练依赖 CUDA 扩展，Mac 本地无法作为主训练平台，需要云端/服务器 GPU 支撑。

## 遇到的问题与处理

| # | 问题 | 处理 |
|---|------|------|
| 1 | Conda 自动建环境失败（ToS 未接受） | 改用 venv，环境创建成功并可运行 toy 实验 |
| 2 | Kaggle 命令执行方式错误（shell vs python） | 明确使用 `!command` 或 `%%bash` 运行 shell 命令；下周按统一脚本执行 |
| 3 | toy 输出难以直观看懂 | 确认 toy 只是概念验证（随机高斯云），不用于论文展示；论文可视化应以 A1 的真实渲染对比图与指标为准 |

## 下周计划（可验收）

1. **在 Kaggle（或 Colab）完成 3DGS A1 smoke run**：
   - 选定一个场景（lego）
   - 跑通：train（~3000 iter）→ render → metrics
   - 记录：命令行、GPU 型号、训练耗时、PSNR/SSIM/LPIPS、test 视角渲染对比图
2. 跑一次完整迭代（或更长迭代）得到可用 baseline 结果，并整理为"实验记录表 + 结果截图包"。
3. 输出一页技术路线与评测指标说明（用于开题报告/中期检查材料沉淀）。

## 本周自评

- **进度**：完成路线决策与概念验证，但官方 3DGS 闭环尚未跑通（下周必须补齐）。
- **风险**：云端环境依赖/编译问题可能耗时；应优先保证 smoke run 跑通，再扩展迭代数与对比方法。
